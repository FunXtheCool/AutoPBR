namespace AutoPBR.App.Services;

/// <summary>Stable ids for <see cref="IBackgroundTaskSink"/> so the UI can map localized labels.</summary>
public static class BackgroundTaskIds
{
    public const string MaterialTags = "material-tags";
    public const string ExploreCache = "explore-cache";
}
