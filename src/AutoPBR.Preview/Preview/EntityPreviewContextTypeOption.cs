namespace AutoPBR.Preview;

/// <summary>One selectable preview placement context (e.g. hanging-sign attachment).</summary>
public sealed record EntityPreviewContextTypeOption(string Id, string DisplayName, bool IsDefault);
